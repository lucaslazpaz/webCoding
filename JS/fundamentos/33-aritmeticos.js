const [a, b, c, d] = [3, 5, 1, 15]
//Operadores aritmeticos
const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

//'-divisao' é um operador unário.
console.log(soma, subtracao, multiplicacao, -divisao, modulo)